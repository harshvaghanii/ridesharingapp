server:
  port: 8081


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?useSSL=false
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true